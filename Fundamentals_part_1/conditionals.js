 
// Taking Decisions: if / else Statements

// const age = 20;
// if (age >= 18) {
//   console.log('Sarah can start driving license 🚗');
// } else {
//   const yearsLeft = 18 - age;
//   console.log(`Sarah is too young. Wait another ${yearsLeft} years :)`);
// }


/*

function printRightAngledTriangle(n) {
  // Outer loop controls the number of rows (from 1 to n)
  for (let i = 1; i <= n; i++) {
    let row = ""; // Initialize an empty string for each row

    // Inner loop controls the number of stars in the current row (from 1 to the current row number 'i')
    for (let j = 1; j <= i; j++) {
      row += "* "; // Add a star and a space to the row string
    }

    console.log(row); // Print the complete row to the console
  }
}

// Call the function to print a triangle with 5 rows
printRightAngledTriangle(5);

*/




// To run some part of our code we use conditions 
// for based on our decision we want to run a line of code 
// we will use if else statemetns 0r (conditions )

// eligiblility 

// let age = 10;

// if(age >=18){
//   console.log("Sarah can drive!!")
// }else{
//    console.log(`${18-age} years are left `)
// }

// to check the century 

let birthYear = 2000;
let century;
if(birthYear <= 2000){
  century = 20;
  console.log(century)
}else {
  century = 21
  console.log(century)
}








